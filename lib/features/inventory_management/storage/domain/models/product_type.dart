/// Represents a type of product in the inventory management system.
class ProductType {
  final String name;

  /// Creates a new [ProductType] with the given [name].
  ProductType({required this.name});
}